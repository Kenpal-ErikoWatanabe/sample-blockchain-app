# `smart_contract/.env` としてコピーして使う（`.env` は Git に含めない）
#   cp .env.example .env
#
# 本物の URL や秘密鍵は .env にだけ書く。.env.example には載せない（Git にコミットされるため）。

# Alchemy などの Sepolia HTTPS RPC（URL に API キーが含まれる）
SEPOLIA_RPC_URL=

# デプロイや --network sepolia 実行時に使うアカウントの秘密鍵（0x 付きでも無しでも可なことが多い）
SEPOLIA_PRIVATE_KEY=
